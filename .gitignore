# Downloaded font release, re-fetched on demand by scripts/build_fonts.py.
.cache/

# Intermediate photo crops. The committed portrait.svg is the deliverable;
# these are just what it was made from.
.work/

# Local screenshots from scripts/preview.py.
.preview/

# Source photographs. Deliberately not committed: el dibujo ASCII es algo que
# eliges publicar, la foto original no. Borra estas líneas si de verdad quieres
# la foto cruda en un repositorio público.
*.jpg
*.jpeg
*.heic
photo.*

# La excepción: la foto de la que sale portrait.svg. No es tuya ni es privada,
# es de Wikimedia con licencia libre, y sin ella el dibujo no se puede volver
# a generar. Su autoría está en content.json.
!assets/portrait-source.jpg

__pycache__/
*.pyc
.venv/
venv/
